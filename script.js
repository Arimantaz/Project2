const x = 15
 const y = 15
 if (x >= y) {
    alert ("Jegele");} else {
        alert ("blogai");
    }
    if (x===y) {
        alert ("Reiks taupyt!")
    }
// if ((x-5)< y) { alert ("blogai");}

console.log ("operacija baigta");